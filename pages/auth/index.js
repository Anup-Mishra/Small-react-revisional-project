import React from 'react';

const authIndexPage = () => (
    <div>
        <h1>The Auth Index Page</h1>
    </div>
);

// authIndexPage.getInitialProps. in these way we can use getInitialProps for functional components.

export default authIndexPage;